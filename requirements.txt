# Flask and web framework dependencies
Flask
Flask-CORS

# PostgreSQL database adapter (psycopg3)
psycopg[binary,pool]

# Authentication and security
bcrypt
PyJWT

# Environment variables (for local development)
python-dotenv
gunicorn

